name = "orange-pine-933a"
main = "worker.js"
compatibility_date = "2026-08-20"

# Note: Removed legacy [assets] section (run_worker_first) because Wrangler v3
# does not support it. If you still need the old assets behavior, deploy with
# wrangler@2 instead: `npm i -g wrangler@2` and then `wrangler publish`.
